name: "First file"

on:
  workflow_dispatch:
    inputs:
      name:
        description: "My name"
        required: true
        type: string
    

jobs:
  execute:
    runs-on: ubuntu-latest

    steps:
      - name: "Greetings"
        run: echo "Hello, world"
